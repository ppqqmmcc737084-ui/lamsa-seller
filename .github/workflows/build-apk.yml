name: Build APK

on:
  workflow_dispatch:
  push:
    branches: [main]

defaults:
  run:
    working-directory: lamsa_seller

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.5'
          channel: 'stable'
      - run: flutter pub get
      - run: flutter build apk --release
      - uses: actions/upload-artifact@v4
        with:
          name: lamsa-seller-apk
          path: lamsa_seller/build/app/outputs/flutter-apk/app-release.apk